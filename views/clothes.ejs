<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wardrobe Wizard - Clothes Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar {
            margin-bottom: 20px;
        }
        .container h1 {
            margin-bottom: 20px;
        }
        .container p {
            font-size: 18px;
        }
        .img-fluid {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>

<body>
    <%- include('navbar', { username: username }) %>
   

    <div class="container mt-5">
        <h1><%= item.type_of_clothes %></h1>
        <img src="/uploads/<%= item.image %>" alt="<%= item.type_of_clothes %>" class="img-fluid">
        <p>Size: <%= item.size %></p>
        <p>Color: <%= item.color %></p>
        <p>Brand: <%= item.brand %></p>
        <a href="/home?username=<%= username %>" class="btn btn-primary mt-3">Back to Closet</a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
